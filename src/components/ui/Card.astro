---
const { as = "div", className = "" } = Astro.props as {
  as?: keyof HTMLElementTagNameMap;
  className?: string;
};
const Tag = as;
const base =
  "rounded-lg border border-border bg-card text-fg shadow-sm";
---
<Tag class={`${base} ${className}`.trim()}>
  <slot />
</Tag>