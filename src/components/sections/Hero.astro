
---
// Hero Section with modern design inspired by premium portfolios
---

<section id="home" class="relative min-h-screen flex items-center justify-center overflow-hidden">
  <!-- Background Effects -->
  <div class="absolute inset-0 bg-gradient-to-br from-blue-500/5 via-gray-900 to-purple-500/5"></div>
  <div class="absolute inset-0 bg-grid-pattern opacity-20"></div>
  
  <!-- Content Container -->
  <div class="relative z-10 max-w-6xl mx-auto px-6 text-center">
    <!-- Greeting -->
    <div class="mb-8 animate-fade-in-up" style="animation-delay: 0.2s;">
      <span class="inline-block px-4 py-2 bg-gray-800/50 backdrop-blur-sm rounded-full text-sm font-medium text-gray-400 border border-gray-700">
        👋 Hello, I'm
      </span>
    </div>
    
    <!-- Main Heading -->
    <h1 class="text-4xl md:text-6xl lg:text-7xl font-bold mb-6 leading-tight text-white animate-fade-in-up" style="animation-delay: 0.4s;">
      <span class="block">Sanjay</span>
      <span class="holographic block">Dhandapani</span>
    </h1>
    
    <!-- Profile Image -->
    <div class="mb-8 animate-fade-in-up" style="animation-delay: 0.5s;">
      <div class="relative w-32 h-32 mx-auto mb-6">
        <img 
          src="/portfolio_headshot.png" 
          alt="Sanjay Dhandapani - Full Stack Developer" 
          class="w-full h-full rounded-full object-cover border-4 border-blue-500/30 shadow-xl hover:shadow-2xl transition-all duration-300 hover:scale-105"
          onError="this.src='https://via.placeholder.com/128x128/3B82F6/FFFFFF?text=SD'"
        />
        <div class="absolute inset-0 rounded-full bg-gradient-to-tr from-blue-500/20 to-purple-500/20 animate-pulse"></div>
      </div>
    </div>
    
    <!-- Role/Title -->
    <div class="mb-8 animate-fade-in-up" style="animation-delay: 0.6s;">
      <h2 class="text-xl md:text-2xl lg:text-3xl text-gray-400 font-light">
        Full Stack Developer & Software Engineer
      </h2>
      <p class="text-base md:text-lg text-gray-400 mt-4 max-w-2xl mx-auto">
        Passionate about building innovative web applications and scalable solutions. Specialized in React, Node.js, TypeScript, and modern development practices.
      </p>
    </div>
    
    <!-- CTA Buttons -->
    <div class="flex flex-col sm:flex-row gap-4 justify-center items-center mb-12 animate-fade-in-up" style="animation-delay: 0.8s;">
      <a 
        href="#projects" 
        class="btn-primary group inline-flex items-center gap-2 px-8 py-4 rounded-xl font-semibold transition-all duration-300 hover:scale-105"
      >
        View My Work
        <svg class="w-5 h-5 transition-transform group-hover:translate-x-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"></path>
        </svg>
      </a>
      
      <a 
        href="#contact" 
        class="btn-secondary inline-flex items-center gap-2 px-8 py-4 rounded-xl font-semibold transition-all duration-300 hover:scale-105"
      >
        Get In Touch
      </a>
    </div>
    
    <!-- Quick Stats -->
    <div class="grid grid-cols-1 sm:grid-cols-3 gap-8 max-w-2xl mx-auto animate-fade-in-up" style="animation-delay: 1s;">
      <div class="text-center">
        <div class="text-2xl md:text-3xl font-bold text-blue-500">3+</div>
        <div class="text-sm text-gray-400">Years Experience</div>
      </div>
      <div class="text-center">
        <div class="text-2xl md:text-3xl font-bold text-blue-500">50+</div>
        <div class="text-sm text-gray-400">Projects Completed</div>
      </div>
      <div class="text-center">
        <div class="text-2xl md:text-3xl font-bold text-blue-500">15+</div>
        <div class="text-sm text-gray-400">Technologies</div>
      </div>
    </div>
  </div>
  
  <!-- Scroll Indicator -->
  <div class="absolute bottom-8 left-1/2 transform -translate-x-1/2">
    <div class="w-6 h-10 border-2 border-gray-400 rounded-full flex justify-center">
      <div class="w-1 h-3 bg-gray-400 rounded-full mt-2 animate-bounce"></div>
    </div>
  </div>
  
  <!-- Floating Elements -->
  <div class="absolute top-20 left-10 w-4 h-4 bg-blue-500/30 rounded-full animate-pulse"></div>
  <div class="absolute top-40 right-20 w-6 h-6 bg-purple-500/30 rounded-full animate-pulse" style="animation-delay: 1s;"></div>
  <div class="absolute bottom-40 left-20 w-3 h-3 bg-blue-500/40 rounded-full animate-pulse" style="animation-delay: 2s;"></div>
</section>

<style>
  .animate-fade-in-up {
    opacity: 0;
    transform: translateY(30px);
    animation: fadeInUp 0.8s ease-out forwards;
  }
  
  @keyframes fadeInUp {
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
  
  .holographic {
    background: linear-gradient(45deg, #3b82f6, #8b5cf6, #06b6d4, #10b981);
    background-size: 400% 400%;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    animation: holographic 4s ease-in-out infinite;
  }
  
  @keyframes holographic {
    0%, 100% { background-position: 0% 50%; }
    50% { background-position: 100% 50%; }
  }
  
  .bg-grid-pattern {
    background-image: 
      linear-gradient(rgba(255, 255, 255, 0.1) 1px, transparent 1px),
      linear-gradient(90deg, rgba(255, 255, 255, 0.1) 1px, transparent 1px);
    background-size: 50px 50px;
  }
  
  .btn-primary {
    @apply shadow-lg hover:shadow-xl;
    background: linear-gradient(45deg, rgb(59, 130, 246), rgb(147, 51, 234));
    color: white;
  }
  
  .btn-secondary {
    @apply bg-transparent border-2 hover:text-white;
    border-color: rgb(59, 130, 246);
    color: rgb(59, 130, 246);
  }
  
  .btn-secondary:hover {
    background-color: rgb(59, 130, 246);
  }
  
  /* Dark mode adjustments */
  :global(.dark) .bg-grid-pattern {
    background-image: 
      linear-gradient(rgba(255, 255, 255, 0.05) 1px, transparent 1px),
      linear-gradient(90deg, rgba(255, 255, 255, 0.05) 1px, transparent 1px);
  }
</style>
