---
// Skills Section with interactive skill cards
---

<section id="skills" class="py-24 px-6 relative overflow-hidden">
  <!-- Background Effects -->
  <div class="absolute inset-0 bg-gradient-to-l from-primary/5 via-background to-accent/5"></div>
  
  <div class="max-w-6xl mx-auto relative z-10">
    <!-- Section Header -->
    <div class="text-center mb-16">
      <span class="inline-block px-4 py-2 bg-muted/50 backdrop-blur-sm rounded-full text-sm font-medium text-muted-foreground border border-border mb-4">
        Technical Skills
      </span>
      <h2 class="text-3xl md:text-5xl font-bold mb-6">
        My <span class="holographic">Toolkit</span>
      </h2>
      <p class="text-xl text-muted-foreground max-w-3xl mx-auto">
        I constantly expand my technical toolkit to stay current with industry trends
      </p>
    </div>
    
    <!-- Skills Description -->
    <div class="max-w-4xl mx-auto mb-16">
      <div class="bg-card/50 backdrop-blur-sm rounded-2xl p-8 border border-border">
        <p class="text-lg text-muted-foreground leading-relaxed mb-6">
          I constantly expand my technical toolkit to stay current with industry trends. Technologies I'm passionate about working with:
        </p>
        
        <!-- Frontend Development -->
        <div class="mb-8">
          <h3 class="text-xl font-bold text-foreground mb-4 flex items-center gap-3">
            <span class="w-2 h-6 bg-primary rounded-full"></span>
            Frontend Development
          </h3>
          <p class="text-muted-foreground leading-relaxed">
            <span class="font-semibold text-primary">HTML5, CSS3, JavaScript (ES6+), Bootstrap</span> - for creating responsive, accessible user interfaces
          </p>
        </div>
        
        <!-- Backend Technologies -->
        <div class="mb-8">
          <h3 class="text-xl font-bold text-foreground mb-4 flex items-center gap-3">
            <span class="w-2 h-6 bg-accent rounded-full"></span>
            Backend Technologies
          </h3>
          <p class="text-muted-foreground leading-relaxed">
            <span class="font-semibold text-accent">Java, REST APIs, JWT authentication, Microservices</span> - building scalable server-side solutions
          </p>
        </div>
        
        <!-- Database Management -->
        <div class="mb-8">
          <h3 class="text-xl font-bold text-foreground mb-4 flex items-center gap-3">
            <span class="w-2 h-6 bg-secondary rounded-full"></span>
            Database Management
          </h3>
          <p class="text-muted-foreground leading-relaxed">
            <span class="font-semibold text-secondary">MySQL</span> - designing efficient data structures and optimizing query performance
          </p>
        </div>
        
        <!-- Programming Foundations -->
        <div class="mb-8">
          <h3 class="text-xl font-bold text-foreground mb-4 flex items-center gap-3">
            <span class="w-2 h-6 bg-primary rounded-full"></span>
            Programming Foundations
          </h3>
          <p class="text-muted-foreground leading-relaxed">
            <span class="font-semibold text-primary">C, C++</span> - strong algorithmic thinking and problem-solving skills
          </p>
        </div>
        
        <!-- Currently Exploring -->
        <div class="mb-6">
          <h3 class="text-xl font-bold text-foreground mb-4 flex items-center gap-3">
            <span class="w-2 h-6 bg-accent rounded-full"></span>
            Currently Exploring
          </h3>
          <p class="text-muted-foreground leading-relaxed">
            Modern frameworks and cloud technologies to enhance my full-stack capabilities
          </p>
        </div>
      </div>
    </div>
    
    <!-- Additional Info -->
    <div class="text-center">
      <div class="inline-flex items-center gap-2 px-6 py-3 bg-muted/50 backdrop-blur-sm rounded-xl border border-border">
        <span class="text-sm text-muted-foreground">
          💡 Always learning and exploring new technologies
        </span>
      </div>
    </div>
  </div>
</section>

<style>
  .holographic {
    background: linear-gradient(45deg, #3b82f6, #8b5cf6, #06b6d4, #10b981);
    background-size: 400% 400%;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    animation: holographic 4s ease-in-out infinite;
  }
  
  @keyframes holographic {
    0%, 100% { background-position: 0% 50%; }
    50% { background-position: 100% 50%; }
  }
  
  .skill-category {
    @apply bg-card/50 backdrop-blur-sm rounded-2xl p-8 border border-border;
  }
  
  .skill-card {
    @apply backdrop-blur-sm rounded-xl p-4 border transition-all duration-300 hover:scale-105 hover:shadow-lg cursor-pointer;
    background-color: rgba(17, 24, 39, 0.8);
    border-color: rgb(75, 85, 99);
  }
  
  .skill-card:hover {
    border-color: rgba(59, 130, 246, 0.5);
  }
  
  .skill-icon {
    @apply text-2xl mb-2 block text-center;
  }
  
  .skill-name {
    @apply font-semibold text-center text-sm mb-1 text-white;
  }
  
  .skill-level {
    @apply text-xs text-center text-gray-400;
  }
  
  .skill-card:hover .skill-level {
    @apply text-primary;
  }
  
  /* Hover effects */
  .skill-card:hover {
    background: linear-gradient(135deg, hsl(var(--primary) / 0.1) 0%, hsl(var(--accent) / 0.1) 100%);
  }
</style>

<script>
  // Add intersection observer for skill cards animation
  const skillCards = document.querySelectorAll('.skill-card');
  
  const observer = new IntersectionObserver((entries) => {
    entries.forEach((entry, index) => {
      if (entry.isIntersecting) {
        setTimeout(() => {
          entry.target.style.opacity = '1';
          entry.target.style.transform = 'translateY(0)';
        }, index * 100);
      }
    });
  });
  
  skillCards.forEach((card) => {
    card.style.opacity = '0';
    card.style.transform = 'translateY(20px)';
    card.style.transition = 'opacity 0.6s ease, transform 0.6s ease';
    observer.observe(card);
  });
</script>
